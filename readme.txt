// interessante : running a compiled build 
// "npm run build" -> to build the solution

// interessante : 
// "npx serve -s build" - serve the content of the build from a "temporary" server on port 5000
